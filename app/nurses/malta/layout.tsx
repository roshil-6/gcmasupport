import { nurseCountryMetadata } from '@/lib/seo-nurse-countries'

export const metadata = nurseCountryMetadata('malta')

export default function NursesMaltaLayout({ children }: { children: React.ReactNode }) {
  return children
}
