import { nurseCountryMetadata } from '@/lib/seo-nurse-countries'

export const metadata = nurseCountryMetadata('new-zealand')

export default function NursesNewZealandLayout({ children }: { children: React.ReactNode }) {
  return children
}
