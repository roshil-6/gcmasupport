import { nurseCountryMetadata } from '@/lib/seo-nurse-countries'

export const metadata = nurseCountryMetadata('uae')

export default function NursesUaeLayout({ children }: { children: React.ReactNode }) {
  return children
}
