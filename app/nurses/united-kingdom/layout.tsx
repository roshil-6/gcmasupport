import { nurseCountryMetadata } from '@/lib/seo-nurse-countries'

export const metadata = nurseCountryMetadata('united-kingdom')

export default function NursesUnitedKingdomLayout({ children }: { children: React.ReactNode }) {
  return children
}
